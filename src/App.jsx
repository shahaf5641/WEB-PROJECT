import React from 'react'
import AppRouter from './approuter'
 
// The App component serves as the root component of the application.
// It is responsible for rendering the AppRouter, which manages the application's routing.
// The App component is the entry point of the application and wraps the entire routing system.
export default function App() {
  return <AppRouter/>
}